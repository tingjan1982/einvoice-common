pluginManagement {
    plugins {
        id 'org.springframework.boot' version '2.6.0'
        id 'io.spring.dependency-management' version '1.0.10.RELEASE'
    }
}

rootProject.name = 'einvoice-common'

